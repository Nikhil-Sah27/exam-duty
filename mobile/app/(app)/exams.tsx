import PlaceholderScreen from "@/shared/ui/PlaceholderScreen";

// PLACEHOLDER — owned by the Exams screen agent.
export default function ExamsScreen() {
  return <PlaceholderScreen name="Exams" />;
}
