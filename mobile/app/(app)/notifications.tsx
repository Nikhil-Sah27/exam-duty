import PlaceholderScreen from "@/shared/ui/PlaceholderScreen";

// PLACEHOLDER — owned by the Notifications screen agent.
export default function NotificationsScreen() {
  return <PlaceholderScreen name="Notifications" />;
}
