# Base URL of the backend API, including the /api prefix.
# A phone cannot reach the dev machine's localhost — use the LAN IP
# (`ipconfig getifaddr en0`) or an ngrok URL. See README.md.
EXPO_PUBLIC_API_URL=http://192.168.0.2:5001/api
