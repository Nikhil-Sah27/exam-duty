# Base URL of the backend API, including the /api prefix.
# A phone cannot reach the dev machine's localhost — use the LAN IP or an
# ngrok URL. See README.md.
EXPO_PUBLIC_API_URL=http://192.168.1.5:5091/api
