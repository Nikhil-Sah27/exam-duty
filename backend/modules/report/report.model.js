// Report module does not require a persistent model.
// Reports are generated from existing data (exams, duties, users).
// A model can be added later if report caching/scheduling is needed.

module.exports = {};
